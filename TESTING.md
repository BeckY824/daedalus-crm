# 上线前测试执行报告

对应测试方案《学员 CRM 上线测试方案》（2026-08-28）。
本文记录**实际执行结果**与发现的问题，而非计划。

## 运行方式

```bash
npm test               # 单元 + Server Action（约 2 秒）
npm run test:e2e       # E2E，dev server（约 1 分钟）
npm run test:e2e:prod  # E2E，生产构建（next build + next start，约 1 分钟 + 构建时间）
npm run test:all       # 单元 + E2E
```

两套都自带环境准备，不需要任何手工步骤：

- 单元测试跑在 `prisma/test.db`，建表由 `tests/setup-db.ts` 自动完成；
  Prisma 客户端不进版本库，缺失时会自动 `generate`
- E2E 跑在 `prisma/e2e.db`，由 `e2e/global-setup.ts` 每轮重建并写入账号，
  dev server 由 Playwright 自己拉起在 3100 端口

两者都不碰开发库与线上库。清库顺序统一在 `tests/reset.ts`，加了新表只改这一处。

## 执行结果：单元 141 条 + E2E 37 条，全部通过

| 文件 | 条数 | 覆盖 |
|---|---|---|
| `tests/attribution.test.ts` | 14 | 归属算法全分支 |
| `tests/actions.test.ts` | 12 | 数据层行为：级联、查重窗口、枚举、转交 |
| `tests/guards.test.ts` | 7 | 修复项的回归锁，含 Dockerfile 构建期密钥 |
| `tests/concurrency.test.ts` | 49 | 多人同时操作、签约查重、退单回退、批量条数 |
| `tests/stress.test.ts` | 3 | 五人极限并发 |
| `tests/audit.test.ts` | 10 | 操作留痕 |
| `tests/migrations.test.ts` | 8 | 迁移机制护栏、备份与恢复脚本约束 |
| `tests/security.test.ts` | 18 | 登录限流、XFF 取值、错误页不外泄 |
| `tests/csv.test.ts` | 17 | 导出的编码、转义、公式注入 |
| `e2e/smoke.spec.ts` | 9 | 线索 → 学员 → 跟进 → 签约主链路 + CSV 导出 |
| `e2e/multiuser.spec.ts` | 6 | 方案 A / D / E 组 |
| `e2e/security.spec.ts` | 3 | 存储型 XSS、登录限流 |
| `e2e/ui-walkthrough.spec.ts` | 19 | UI 走查（空状态、校验可见性、响应式、分页、后退等） |

`concurrency.test.ts` 与 `stress.test.ts` **直接调真实的 Server Action**
（用 `vi.mock` 顶掉登录与缓存失效两个只有 Next 运行时才有的依赖）。
并发闸门是否成立取决于判断和写入是不是同一条语句，绕过 action 就测不到这一点。

E2E 有两种跑法：默认用 dev server，`E2E_PROD=1` 改用生产构建
（`next build` + `next start`）。后者验的是和线上同构的产物——路由缓存更激进、
没有开发覆盖层、`AUTH_SECRET` 走生产分支。**14 条在两种模式下都全绿**。

`tsc --noEmit` 0 错误，`eslint` 0 错误（14 个 warning 全在 `seed-demo.ts`
与 `scripts/test-multiuser.mjs`，与本轮改动无关）。

---

# 第一轮：从「界面点不出来但接口能做到」入手

| # | 问题 | 级别 |
|---|---|---|
| 1 | 删除学员会静默抹掉下游业绩归属（Prisma 自引用默认 `SetNull`） | P0 |
| 2 | `AUTH_SECRET` 缺失时静默回落到硬编码默认值，用公开密钥签发登录凭证 | P0 |
| 3 | 跟进/决策状态无白名单 | P1 |
| 4 | 签约金额可为负数或零 | P1 |
| 5 | 批量操作提示不反映实际条数 | P2 |

方案列的三个 P0 阻塞项在方案成文前已修完，复核无误。

---

# 第二轮：两个人同时操作、互相没告知

起因是一个具体问题——**两人同时打开并修改同一条学员信息会怎样**。
顺着这条线把整套系统同类的地方过了一遍。

## 1. 并发编辑静默覆盖 → 字段级合并（P0）

甲打开编辑框还没保存，乙改完同一条并先保存。甲点保存时提交的是
**打开那一刻的一整套旧值**，把乙的改动整体盖掉，两人都收不到提示。

**做法**：乐观锁 + 字段级合并。表单带上打开时的 `updatedAt`（版本号）
和一整份快照（base）。保存时版本号进 `where`，判断与写入落在同一条
`updateMany` 里，中间没有可插入的窗口。版本号对不上时，用 base 算出
「对方改了哪些字段」和「我改了哪些字段」：

- **没有交集** → 只写我改的那几个字段，直接合并，用户无感
- **有交集** → 拦下，只列真正撞车的字段，另外告知对方还动了什么

改推荐人时归属三件套（`channelId` / `attributionChannelId` /
`channelOwnerId`）跟着一起写，否则合并会留下一条算不出归属的记录。

**过程中修掉的两个自身缺陷**：

- **版本号同毫秒失效**。`updatedAt` 只精确到毫秒，两次保存落进同一毫秒时
  版本号不变，旧版本号会再次匹配成功，闸门形同虚设。测试跑 40 轮，
  第 3 轮就撞出来。修法是保存时强制推进一格（`max(now, 旧版本 + 1)`）。
- **把用户自己改的字段当成冲突**。最初 `conflictingFields` 比的是
  「库里现值 vs 我要提交的值」——我自己改的当然对不上。叠加高频路径
  （录一条跟进会同步 `lastFollowAt`、推高版本号），结果是：对方只录了跟进、
  根本没碰学员资料，甲的保存也会被拦，提示还自相矛盾：「不冲突」却「已取消」。
  界面确认那一步才发现，因此有了上面的字段级合并。

**刻意没做**：没有加「强制覆盖」按钮（等于把坑重新挖开，只是多点一次）；
没有让 `lastFollowAt` 的同步跳过版本号（那需要把版本号往回写，
有让真正过期的保存蒙混过关的风险，而合并逻辑已经吸收了这个场景）。

## 2. 两人同时转化同一条线索会建出两条学员（P0）

原本「查 `customerId` 是否为空 → 建客户 → 回写线索」三步分开。两人同时点转化，
双方都看到未转化、双方都建档成功；回写只留下后一条的关联，
**另一条学员变成没人知道来历的孤儿记录**，手机号查重也一起穿了过去。

**修复**：三步进同一个事务，用 `updateMany({ where: { customerId: null } })`
做闸门——只有把线索翻过来的那一次会影响到行，另一次影响 0 行直接退出。

## 3. 其余修复

| 问题 | 影响 | 级别 |
|---|---|---|
| 编辑他人的跟进记录会把 `ownerId` 改成自己 | 「谁跟进的」和按人统计的跟进量一起失真，无痕迹。待办、跟进计划同样 | P1 |
| 主要联系人可同时存在多个 | 详情页按 `isPrimary` 倒序取第一条，显示哪个取决于创建顺序，像是对方的修改没生效 | P1 |
| 推荐链可成环 | 只挡了「推荐人是自己」，两步就能绕过去；成环后归属计算自相矛盾且不报错 | P1 |
| 停用成员漏转线索与渠道负责人 | 线索留在停用的人名下等于丢了；渠道之后新带来的学员继续算到离职的人头上 | P1 |
| 可停用或降级最后一个管理员 | 做完当场看不出问题，下次要加人时才发现没人能管，只能改库救 | P1 |
| 编辑框里勾掉「在职」会跳过转交 | 名下数据留在停用的人名下，而各处下拉只列在职成员 | P1 |
| 已丢单的商机拖回任意阶段会静默复活 | 重新计入漏斗和预测金额，丢单记录凭空消失 | P2 |
| 商机金额可负、概率可越界、阶段与状态无白名单 | 污染漏斗与加权预测 | P1 |
| 跟进类型/状态无白名单、通话时长可为负 | 污染看板统计 | P1 |
| 线索状态与来源无白名单 | 同上 | P2 |
| 密码可设成空或过短 | 空密码等于任何人都能登进这个账号 | P1 |
| `deleteChannel` 只查 `channelId`，漏 `attributionChannelId` | 归属落在该渠道上的学员会被置空 | P2 |
| `Alert` 仍用 antd v5 的 `message` prop | v6 已改名 `title`，旧写法刷 deprecation error | P2 |
| 五处调用方吞掉失败结果 | 上面这些校验加上之后，**校验失败界面照样显示「已保存」** | P1 |

---

# 第三轮：按决策收口 + 补齐自动化

## 决策落地（2026-08-29 拍板）

| 决策 | 落地情况 |
|---|---|
| **手机号不加唯一约束**，接受同号多条（家长与学生共用） | 代价是「先查后写」的并发窗口保留。对应用例已从「已知缺陷」改标为「已确认接受的行为」，哪天要加约束就翻转断言 |
| **接受全员可见可改 + 操作留痕** | 权限部分维持现状；**操作留痕未做**，见下方「唯一的阻塞项」 |
| **签约查重：同学员 + 同金额 + 同一天 → 二次录入需确认** | 已实现。弹窗说明已有那笔的金额与日期，确认后仍可录入（续费、分期不受影响）。「同一天」按自然日算 |
| **删除签约时弹窗让人选回退档位** | 已实现。只在删掉最后一笔时询问，提供三档预设加「保持不变」；还有别的签约时不动状态 |

## 上一轮审出的三个残留洞

| 洞 | 处理 |
|---|---|
| `convertLead` 的手机号查重仍是先查后写（跨线索同号并发） | **按决策接受**。根治只有唯一约束，业务上已决定不加 |
| 批量操作单向绕过闸门、不返回实际条数 | 条数已实现：分别回报「真改了几条 / 本来就是几条 / 已不存在几条」，并拒绝转给已停用成员。**版本号闸门刻意不套**——批量是操作人明确勾了行、明确选了值，套了会让批量动不动整批失败，反而没人敢用 |
| 高频假冲突没走过真实界面 | 已走。**确实是问题，而且比预估严重**，见第二轮第 1 条 |

## 冲突提示的界面确认（上一轮欠账）

三个场景都在浏览器里实际跑过：

| 场景 | 实际表现 |
|---|---|
| 真冲突 | 标题 + 正文完整，列出撞车字段，时间按本地时区渲染；**确认对方的改动完好，旧数据没盖上去** |
| 假冲突 | 复现了，文案自相矛盾——**这条促成了字段级合并的改造** |
| 编辑期间被删 | toast「这条学员已被其他人删除，无法保存」，弹窗保持打开不丢输入 |

**这个浏览器面板截不到 modal 图层**（遮罩拍得到、内容拍不到），
所以是直接读 DOM 里渲染出来的文案。对「文案看不看得懂」这个问题，
读到逐字文本反而比截图更有用。

## 方案 A / D / E 组：不需要两人两机

方案把这三组标成「只能手工，需两人两机」。**实际不需要**——Playwright 的
每个 browser context 有独立 Cookie 与缓存，一台机器可以同时挂多个互不干扰的
会话。6 条已全部自动化（`e2e/multiuser.spec.ts`）：

- **A 组**：甲新建、乙刷新可见。另一条把 `staleTimes.dynamic = 60` 的实际行为
  固化下来——**软导航离开再回来会命中路由缓存看到旧列表，硬刷新一定是最新**。
  这是性能取舍不是缺陷，但以前只写在文档里，现在有用例盯着
- **D 组**：两人同时批量改状态，双方都收到带真实条数的提示，不互相锁死；
  一人批量改负责人、另一人正在编辑同一条时，字段级合并让两边改动都保住
- **E 组**：同账号双会话都能正常用；使用中被管理员停用后，下一次操作被踢回
  登录页而不是继续用失效会话

## F 组：五人极限

`tests/stress.test.ts`：5 人各 8 轮真实操作混合（建档 / 录跟进 / 改资料 /
读列表）+ 五人同时批量改状态，断言不出现 `database is locked`。
另加一条直接校验 WAL 与 `busy_timeout` 确实生效——这两项配置被改掉时，
表现是销售随机保存失败且极难复现，值得长期有一条用例盯着。

## E2E 冒烟

`e2e/smoke.spec.ts` 8 条，一条学员从线索走到签约的完整链路。
第 8 条用两个 browser context 验证并发编辑的两种结果（合并 / 拦截），
是单机能给出的最强的多人证据。

搭建时踩到两个坑，都写进了注释：

- **必须用 `localhost` 而非 `127.0.0.1`**。Next 16 dev 会拦截「非同源」的
  dev 资源请求，用 IP 访问导致 JS chunk 全被拒、页面不水合，
  表现为点按钮完全没反应，而错误只出现在服务端日志里
- **antd v6 的 `Alert` 没有 `role="alert"`**，只能按类名找

---

## 部署：第一次挂了，原因是我自己上一轮埋的

第一次 `./scripts/deploy.sh` 在 Docker 构建阶段失败：
`Failed to collect page data for /follow-ups`——报错只说某个路由挂了，
完全不提真正的原因。

根因是第一轮那个 `AUTH_SECRET` fail-fast 修复：`auth.ts` 在**模块顶层**校验密钥
（生产环境缺失即抛错），而 `next build` 跑的就是 `NODE_ENV=production`。
Dockerfile 只给了构建期的 `DATABASE_URL` 占位串，没给 `AUTH_SECRET`，
于是所有 import 了 `requireUser` 的页面在「collect page data」阶段全部失败。

**本地构建一直是绿的**，因为 `.env` 里有 48 位的开发密钥，而 `.env` 在
`.dockerignore` 里——这正是它没被早点发现的原因。

修法沿用 Dockerfile 里 `DATABASE_URL` 已有的同一套做法：构建阶段给一个占位值。
运行阶段是另一个 stage，不继承这个值，真实密钥仍由 compose 注入。
`guards.test.ts` 加了一条回归锁检查这件事（构建段必须有占位值且 ≥32 位，
运行段必须完全不出现 `AUTH_SECRET`）。

Docker 会对这行报 `SecretsUsedInArgOrEnv` 警告，是预期的——那是占位值不是密钥。

### 线上验证结果

| 项 | 结果 |
|---|---|
| 容器状态 | `Up`，日志 `✓ Ready`，无密钥相关报错 |
| 容器内 `AUTH_SECRET` | 64 位，**确认不是构建期占位值** |
| 数据 | 3 个账号，业务数据 0 条（与部署前一致，线上尚未投入使用） |
| `/login` | 200 |
| 未登录访问 `/customers` | 重定向到 `/login` |

部署前已用 `scripts/backup.sh` 拉了快照（`backups/crm-20260829-1658.db`）。

## 备份与恢复演练（2026-08-29 在真实服务器执行）

此前只有备份脚本、没有恢复脚本——这正是「恢复没演过」的原因。
`scripts/restore.sh` 补齐后，趁线上零业务数据完整走了一遍。

**做法**：插入标记数据（1 条学员 + 1 条留痕）→ 备份 → 删掉标记模拟数据丢失
→ `CONFIRM=RESTORE ./scripts/restore.sh <备份>` → 校验。

| 项 | 结果 |
|---|---|
| 全程耗时 | **51 秒** |
| 服务不可用窗口 | 约 35 秒（停容器 → 换库 → 起容器） |
| 数据恢复 | 学员 0 → 1、留痕 0 → 1，**标记确实回来了** |
| 恢复后探活 | `/login` 200 |
| 恢复前自动备份 | 有（脚本第 1 步会先备份当前状态，恢复错了还能回来） |

演练中撞出并修掉的两个真问题：

- **备份文件名只精确到分钟**，同一分钟内备份两次会静默覆盖前一次。
  当时先备了干净状态、一分钟内又备了一份带标记的，干净那份直接没了，
  脚本一声不吭。备份互相覆盖是最不该发生的事——那正是出问题时唯一的退路。
  已改为精确到秒 + 同名拒绝覆盖，并有用例锁住。
- **`Caddyfile` 从来没有被 `deploy.sh` 同步过**，改了本地也不会生效。
  反代配置和应用是一起演进的（比如 X-Forwarded-For 的处理），已加入同步并自动 reload。

另外验证了迁移机制在真实存量库上生效：恢复的是迁移前的备份（无 `AuditLog` 表），
容器启动日志出现 `→ 迁移 001-audit-log.sql`，表与索引都补上了。

## 操作留痕与迁移机制（已完成）

决策「全员可见可改 + 操作留痕」已落地。留痕表要新建，而线上原本没有增量迁移
机制——`docker-entrypoint.sh` 只在库不存在时建表。现按评审意见做成 `migrations/`
目录：按序号命名的**幂等** SQL，容器每次启动按序全部重跑，不记录执行进度
（少一套状态就少一处会和真实表结构对不上的地方）。全新安装仍走构建期生成的
`schema.sql`，两条路径分开且会收敛到同一结果。

护栏在 `tests/migrations.test.ts`：文件必须按序号命名、禁止
`ALTER`/`DROP`/`DELETE`/`UPDATE`/`INSERT`、建表建索引必须带 `IF NOT EXISTS`、
连跑两遍不报错；另检查 `schema.prisma` 有 `AuditLog` 且不建外键、
entrypoint 会执行迁移、Dockerfile 会把 `migrations` 拷进运行镜像。

留痕表刻意不与 `User` 建外键、姓名冗余存储：日志只增不改不删，
不该因为成员被删就丢失，也因此永远不需要 `ALTER`。

## 归属算法覆盖率与自动关卡（2026-08-30）

门禁里那条「归属算法分支覆盖 100%」，此前一直是**凭印象**在说——项目根本没装
覆盖率工具，无法证明。装上 `@vitest/coverage-v8` 实测，第一次的数字是：

| 指标 | 实测 | |
|---|---|---|
| 语句 | 92.30%（24/26） | |
| 分支 | **78.94%（15/19）** | 离 100% 差了 4 个 |
| 函数 | 66.66%（2/3） | 有一整个函数没被测到 |

漏的是 `attributionLabel`——列表与详情共用的归属展示口径，**一条测试都没有**。
它显示错了不会报错，只会让人看着错的归属对象做判断。另有 `wouldCreateCycle`
的「推荐人是自己」早退分支也没测到（`saveCustomer` 另有一道校验挡在前面，
但这个函数是公开导出的）。

补上 5 条用例后：

| 指标 | 实测 |
|---|---|
| 语句 | **100%（26/26）** |
| 分支 | **100%（19/19）** |
| 函数 | **100%（3/3）** |
| 行 | **100%（22/22）** |

复跑：`npm run test:coverage`。

### 自动关卡（此前完全没有）

仓库无远端，GitHub Actions 用不上，改为本地 git 钩子 `hooks/pre-push`：
`tsc --noEmit` + `eslint` + `vitest run`，实测 **8.4 秒**，任一失败即中断推送。
启用方式 `git config core.hooksPath hooks`，已写进 README。

E2E 不进钩子（要起 dev server、跑三分钟），维持「合并后手跑 + 部署后线上复验」。

**这一条补的是当初门禁第 1 项欠的一半**：类型错误早就清零了，但「接入 CI」一直没做，
三个类型错误能在代码里躺很久正是因为没有任何自动关卡。

## 其余已知行为（已确认，非待修）

| 项 | 说明 |
|---|---|
| 手机号可同号多条 | 业务决策，见上 |
| **无存量数据需要导入** | 2026-08-29 确认：没有历史数据，「手工录入 + 查重护栏」就是方案本身。存量入库这项正式关闭 |
| 无数据级权限 | 业务决策，配套的留痕待迁移机制确定 |
| 软导航最长滞后 60 秒 | 性能取舍，已有用例固化实际行为，写进使用说明即可 |
| 跟进记录的「标题」 | 已改为选填，填了就在时间线上显示 |
| 商机赢单时间用 `updatedAt` 近似 | 模型里没有单独的赢单时间字段，加字段要 `ALTER TABLE`，与「只增不改」的迁移约定冲突。商机赢单后通常不再改动，实践中够用，口径已标在卡片上 |

## UI 走查（2026-08-29）

手工清单里**机器能客观判定**的部分已自动化（`e2e/ui-walkthrough.spec.ts`，19 条），
判定标准都可证伪：空状态有没有引导文字、校验提示的**实际高度是否大于 0**
（不只看有没有元素）、长文本会不会撑出横向滚动条、窄屏 1024 与手机 390 视口、
分页、后退、刷新保留筛选、连点两次不重复提交。每个页面都留了截图在
`test-results/走查/`。走查用的模拟数据（61 学员 / 18 线索 / 15 商机，含四级推荐链）
跑完自动清空，只连 E2E 库。

发现并修掉的四处：

1. **手机上每个页面都横向溢出 66px**（456 > 390）。侧栏在窄屏只收到 80px 而不是 0。
   销售从微信点进来第一眼就是一条横向滚动条。
2. **数据首页有三处编出来的数字**：预测销售额卡写死 `delta={18.6}`（零数据也显示
   「较上月 ↑ 18.6%」）；「新增客户」的涨跌写死 ↑ 和绿色还套了 `Math.abs`，
   **下降会显示成上升**；三条小曲线是写死的数组。已改为按最近 8 周真算。
3. **业绩排行的姓名压在进度条上**（格子固定 92px 且不裁剪）。
4. **「本月/本季」下拉完全没有接线**，选了没反应，卡片上却写着「本月」。已接线。

过程中排除了两个**假发现**，方法上的教训记在这里：dev 模式 StrictMode 下校验文字的
入场动画不结束、高度恒为 0（生产构建正常）；浏览器工具用 `Return` 键名送出的
keydown，其 `key` 是空字符串，看起来像「回车不能提交」。**这类走查必须在生产构建上做。**

## 仍未覆盖

- 真机浏览器差异，尤其**微信内置浏览器**。Playwright 能跑 WebKit 内核，
  但模拟不等于真机，这一项只能人工
- 业务正确性：归属规则与实际提成规则是否一致、状态档位够不够用、
  字段缺不缺——只有用的人知道
- 文案是否说得清、流程符不符合实际带教习惯

以上三类整理在 `docs/手工测试清单.md`，建议随 UAT 一起做。
- 方案第 8 节 B / C 组的手工条目：大部分已被 48 条并发用例覆盖，
  未逐条对照勾选

## 工程约束（2026-08-30 已落地）

本节原为「建议」：把 `npm test`、`tsc --noEmit`、`eslint` 接进提交前钩子或 CI。
现已做成 `hooks/pre-push`，8.4 秒，详见上方「归属算法覆盖率与自动关卡」一节。

`npm run test:e2e` 按原建议留在钩子之外——三分钟太慢，维持
「合并后手跑 + 部署后线上复验」。
