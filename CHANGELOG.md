# 更新记录

给用的人看的。每一版一段人话，只写你会感觉到的变化；改了什么代码看 git log。
官网「检查更新」里的说明和这里同源。

## 未发布

**新增：没有表格也能导——粘一段文字进去**

名单不一定在 Excel 里。更多时候它是微信里转来的一段消息、群里的接龙、会议纪要上的
一串人、名片上抄下来的几行。导入抽屉第一步现在有两条路：「文件」和「**粘一段文本**」。
粘进去，按一下「整理成表格」，AI 把它切成一张表，**从第二步开始和从 Excel 导进来
一模一样**——同样的对列、同样的复核、同样的预览、同样能整批撤销。

**AI 只做一件事：切分。** 不改写、不补全、不翻译，也不把「下周三」算成哪一天
（那是下一步的事，和 Excel 那条路走的是同一套规矩）。每一格都必须是你原文里的原字。

所以我们能替你核对它有没有老实干活，两件事都在你看到表格之前做完：

- **它编的格子会被清空并列给你看。** 原文里找不到的字就是它编的。
  假客户混进库里是看不出来的，还会被打电话
- **原文里有、表里没有的手机号会列出来。** 那多半是有人被漏掉了——
  这是最安静的一种错，不说的话你永远不知道少了谁

**粘进来不会自己跑**，得你按那颗按钮：这一次整理要花一次 AI 次数。
一次最多粘 6000 字，再多就存成 Excel 走「文件」那条路。
文本不传给我们，只发给你自己配的那个模型。没接模型时这条路会直说，
并告诉你「文件」那条路照样能用——那条不花钱、不走网络。

**新增：把手上那份 Excel 导进来**

客户列表页头上多了一颗「导入」，空库时空状态里也有一条。收 .xlsx 和 .csv，
最多一万行。**文件不上传**——在你这台机器的浏览器里读完，直接落进你自己的库。

走五步，每一步都告诉你接下来会发生什么：

- **对列**：左边是你表里的列，右边选它是什么。常见的写法会自动认出来
  （「联系方式」「手机」「电话」都算手机号，「姓名(必填)」这种带注脚的也认）
- **认人只认手机号**，这一列必须指出来。同一个手机号算同一个人。
  按姓名认人不做——重名就是把客户挂到别人名下
- **复核**：只列读不懂的格子。**一格读不懂不会让整行进不来**——
  那一格留空，其余照进。同一列里同一个写法改一次，那一列所有这么写的行一起生效
- **预览**：新建几条、跳过几条、几条进不来。**这几个数就是真正会发生的数**，
  不是估计；表里同号的多行已经先合过了
- **执行**完给你一行结果，外加一颗「撤销这一批」

**表里有我们没有的列怎么办**

比如「微信号」「客户等级」「身份证」。我们不做自定义字段，但也不会默不作声地丢掉——
默认**并进备注**，写成「微信号：xxx」一行一条。对列那一步会告诉你是哪几列，
不想要可以关掉。不这么做的话，你会以为整份表都进来了，几周后才发现微信号一个都没有。

第一行要是表头。要是这份表压根没有表头（第一行就是第一个人），会当场拦下来告诉你，
而不是把第一个人当成表头吃掉。

**手机号已经在库里的那些行只有两个选择：跳过，或者只补空着的字段。**
没有「覆盖」。人录过的东西不该被一份表刷掉。来源渠道也不动——
那会让一位客户静默换主。

**整批可以撤销，导完之后也能撤**

「设置 → 导入记录」里列着最近几批，每批一颗撤销：新建的删掉、补上的还原。
**导入之后你已经改过、或者已经有跟进记录的那几位会留着**，撤销时会告诉你是哪几位——
不然撤销本身就成了第二次事故。

**改：注册页那句「送 30 次」现在说的是实话**

注册赠送一台电脑只送一份（0.39.2 起）。注册页原来只写「送 30 次」，
而同一台电脑上开第二个账号的人登录后只看到 3 次，界面上找不到任何解释。
现在注册页写明「一台电脑只送一份」，设置页显示余额的两处也会在没领到时说清原因。

## 0.39.1（2026-09-19）

全是修，外加左下角那个「AI 任务」改了样子。都是 0.39.0 的面板带出来的。

**修：一个问题，历史里留下两条一模一样的回答**

问完一句，把面板关掉再打开（或者切去别的页面再回来），那一问一答会**再被记一遍**。
翻历史时就是两条一模一样的回答，连用时都一样。模型只跑了一次、也只扣了一次次数
——多出来的是记录本身。现在同一轮只会记一次。

**改：「跑完了叫你一声」，不用再盯着看**

左下角原来是一张常驻的单子，答完了还得你点一下才消失，于是它一直占着地方。现在：

- 跑着的时候只有一行字和一条细进度，**答完变成「答完了」，3 秒自己走**
- 同时问了好几句就缀一个「+N」。想翻完整的，首页那条对话列表本来就在
- **要你做事的不会自己走**：没答上来的、答完了还有一张建议卡等你点确认的，都留着
- **桌面端：你切去别的应用了，答完会发一条系统通知**，点一下把窗口叫回前面。
  人还在窗口里就不弹——左下角那一条已经说了。第一次会问你要通知权限

**修：在功能页问一句，左边那一整栏跟着跑**

右侧面板里答案一长，整个页面被往下滚了半屏——你正在看的那一页标题、卡片全跑出视野。
现在面板自己滚自己的，正文一动不动。

**修：点左下角那条任务，右边的回答当场消失**

你想的是「这条我看过了，划掉」，结果连答案一起划掉了，只好再问一遍。现在只是划掉那一条。

**修：面板里那张建议卡的按钮挤成了两行**

380 宽的面板里，「确认」两个字竖着排。现在放不下会让提示语换行，按钮不再被挤。

## 0.39.0（2026-09-19）

**它现在能回答的问题多了一大类。**

以前 AI 只会用那十一个写死的查法：找客户、列渠道、列线索……
每个查法各认自己那几个参数，参数里没有的就答不了。
于是「联系人里有几位留了微信」（联系人压根没有查法）、
「最久没人动过的那条线索是哪个」（不能排序）、
「9 月签的单子里哪几笔是陈姓学员的」（要跨两张表）——这类问题一概答不上来。

- **新增通用查询**。九张表（客户 / 线索 / 商机 / 签约 / 联系人 / 跟进记录 / 跟进计划 / 任务 / 渠道）
  都能按任意条件找、排序、数个数、按某个字段分组统计，还能跨一张表筛
  （「小红老师带来的学员里，9 月签约的有哪几位」）
- **它查了什么，原样写在过程条上**：「在客户里，找 跟进状态 是 意向较高、且 建档时间
  不早于 2026-09-01，按预计签约时间从小到大排，取 20 条」。
  这不是装饰——一个查错了却报得很自信的数，比「答不出来」隐蔽得多，你得能一眼核对
- **词写错了它会当场纠正**。问「意向高的有几位」（正确的说法是「意向较高」），
  它不会查出 0 条然后告诉你「一位都没有」
- 它**只能读**。和以前一样，改数据仍然要你在建议卡上点确认

**修：AI 偶尔会吐出一串没人看得懂的东西**

有时候回答框里蹦出来的不是答案，是 `<｜｜DSML｜｜ calls>` 这种标记、
或者光秃秃一个工具名、或者一句「我来查一下」然后就没了。
现在这三种都会被当场拦住并重答一遍。

**修：在「数据」页问「新增的客户是谁」答不出来**

那一页原来被限定只能用「查数字」那个工具，而它只给数不给名单，
「是谁」天生答不了。现在每一页都写明：默认那个工具答不了的，换一个。
跟进记录页、跟进计划页也有同样的毛病，一并修了。

**修：AI 回答里的手机号没打码**

网页那个共享工作区（多个团队共用一套账号密码的那个）里，表格显示 `139****1111`，
但问一句 AI 拿到的是完整号码，而且会留在对话记录里。现在 AI 这条路也打码了。
自己部署的实例不受影响——销售要照着这个号打电话。

**修：建议卡点了确认，背后的列表还是旧的**

在客户列表上确认「改成已签约」，卡片说改好了，而那张表还显示着旧状态。现在会跟着刷新。

- 模型选单：填了自己的 API Key 时，只列你填的那个模型（以前会把上一次云端账号的型号也列出来，
  而那些在你自己的接口上并不存在）

## 0.38.2（2026-09-19）

这一版全是修。上一版的面板带出来两个问题，另外把各个页面上「数字和它的名字对不上」的地方查了一遍。

**AI 面板**

- **修：每个页面的对话现在是各自独立的**。以前在线索页问一句，切到客户页、回到首页，那一问那一答跟着你到处走——更麻烦的是它们还会被记进库里**同一条对话**。现在一页一屏，各问各的；记录照样留在首页那条列表里，标题前面带上是在哪一页问的（「线索 · …」），回头找得到
- **修：在功能页问这一页上的事，它会去对的那张表找了**。以前在线索页问「Steven 是哪家公司的」，它会连着四次去搜**客户**表（Steven 在线索表里），一条都查不到，最后卡住。现在每一页都明确告诉它这一页的数据该怎么查——线索页找线索、商机页找商机、渠道页找渠道
- **改：数据页正文里那个「问一个数」撤掉了**。面板现在每一页都常驻，这一页就同屏摆了两个长得一样的框——一个只问数字、出图表，一个是完整的 AI。两件事确实不同，但分不出来的人只会以为坏了一个。少这个框不丢任何能力，数字类问题面板照样答
- **改：Esc 不再收起面板**。它是常驻的一栏，不是弹层。而且面板里的 Esc 本来就有别的用处——答案正在流的时候按它是「打断」，于是想停下一个跑偏的回答会连面板一起收掉。要关面板用 ⌘J，或者它右上角那个 ×
- **修：同一个查询连着调第二次会被拦下来**，并且告诉它换张表或换个条件，而不是让它原地打转到步数用完。以前那种「四行一模一样、什么都没查到」的过程条不会再出现

**数字和它的名字对不上（查了一遍全站）**

- **修：渠道页「直接推荐」和「整条推荐链」以前永远是同一个数**。两列并排摆着，一列还写着「含下游转介绍」，其实算的是同一件事——于是「转介绍到底带来了多少人」这张表一直答不出来。现在「直接」只数这条渠道亲自带来的，链上多出来的那些会写明「转介绍 N」
- **修：首页「逾期跟进」漏了一半**。它只数跟进计划，不数任务；而点进去的那一页两种都算。有几条逾期任务、零条逾期计划的人，首页写着「0 · 都跟上了」，点进去一片红。「近期跟进任务」那张卡同样只认任务，只用跟进计划的人看到的永远是空的——两处都改成和落地页同一个口径，过了时间的会标红「已逾期」
- **修：签约趋势图会跳过没签约的日子**。1 号、5 号、19 号各签一单，图上就是三根挨着的柱子，中间十几天的空白根本不在横轴上，看起来像天天在签。现在每一天都占一个刻度。顺带：**图上点哪儿都能打开明细了**，不用非得戳中那根柱子；点在没签约的那天，它会告诉你这一段确实是空的
- **修：「客单价」除的是笔数，不是人数**。同一位学员续费、补课会签好几笔，那样算出来的是每笔均价。现在按人算，卡片上写清「N 位学员签了 M 笔」
- **修：线索 / 商机 / 联系人 / 跟进四页的「共 N 条」其实是上限**。这四页一次最多取 300 条，而页脚数的是取回来的行——库里有 500 条线索的人看到的是「共 300 条」。现在说实话：「这里只有最近 300 条，库里一共 500 条」
- **修：首页最底下那句「预测销售额 = Σ(进行中商机金额 × 成交概率)」，给的却是没乘概率的数**。那张卡两版之前就撤了，这句话留了下来。删掉——同一个数在那一屏上已经出现两次
- 业绩排行的金额补上了「¥」（以前是一串光秃秃的数字，容易被当成人数），并写明口径：它数的是赢单商机、不限时间，和「本月签约」那张卡不是一回事

## 0.38.1（2026-09-19）

- **AI 面板现在默认就开着**，不用每次点一下或者按 ⌘J。关掉之后它记得住，直到你自己再打开
- **面板和首页的对话列表都能拖宽了**，和左栏一样：拖一下定住、换页还记得、双击回默认
- 面板里那块对话的排版修了：还没问过的时候，提示和输入框在中间，不再是输入框贴着最下沿、上面空一大片
- **修：首页那张「客户趋势分析」图，图例在说谎**。那条写着「新增客户」的线画的其实是**累计**客户数——昨天加了一位，今天这条线还是 1，而按「新增」的说法今天该是 0。改叫「累计客户」，并且在图下面写清两条线各是什么：累计客户 = 截至当天库里一共有多少位；活跃客户 = 其中最近 30 天有过跟进记录的
- 修：⌘K 在有面板的页面上弹不出跳转单了（它先看到了面板里那个输入框）。⌘K 和 ⌘J 是两件事，现在 ⌘K 只认这一页自己的输入框

## 0.38.0（2026-09-19）

**任何一页都能问它一句了**，不用再回首页。

- 右边多了一条窄边，点它或者按 **⌘J**，AI 面板从右边推出来（380 宽）。左栏一格不动，正文跟着收窄——它不盖住你正在看的东西，因为你问的往往就是那些东西
- **它知道你在看哪一页**。在筛着「已签约」的客户列表上问「这些人谁快签了」，那个筛选就是问题的一部分；在张三的记录页问「他什么情况」不用再报名字。带了什么就写在面板顶上一行，不想带点掉就是了——一个看不见的上下文，答歪了没人解释得清为什么
- 切到别的页面，正在跑的那一问跟着你走，不断
- 首页没变：那儿本来就是整页的同一块东西，不会再多出一个小框
- 没配 AI 的话，这条窄边和面板都不存在——不是给你一个点开说「先去配 AI」的空壳

## 0.37.4（2026-09-19）

这一版两件事：更新少点两次、界面把"审美"变成规则。

- **差量更新自动下载了**。以前查到新版要你点一次「更新」才开始下，现在几 MB 的差量包后台自己下好；只有整包（首次装或差量失败）才会等你点。0.24.0 那会儿"先问再下"是因为整包 160 MB，差量之后那个理由不在了
- **退出时顺手装上**。下好的更新不用专门点「重启」——正常退出时自动换上，下次打开就是新版。想立刻要的仍然可以点侧栏的「重启」
- **重启回到原来那一页**。以前每次起来都落回首页，更新完刚看到一半的客户记录没了；现在记得你停在哪
- 界面：全站 79 个各写各的颜色收成一套（语义色 + 八个分类色 + 八个头像色），主蓝只剩一个（以前 #2f6bff 和 antd 的 #1668dc 并存了半年）；邮件沟通那个类型换了个颜色，以前和上门拜访几乎分不出来。规则写在 `docs/DESIGN_SYSTEM.md`，体检清单在 `docs/UI_REVIEW_CHECKLIST.md`，色值散出去 CI 会红
- 修：过程条上明明查了渠道清单却写「没有读取任何记录」（0.37.3 之后加的六个工具它一个都不认识）

## 0.37.3（2026-09-18）

这一版修的是一件更要紧的事：**它会编。**

- **修：它不查库就下结论**。问「我现在有什么渠道」，它一个数据都没查，直接答「查了一下，目前系统里还没有登记任何渠道」——而你的库里明明有一个。你没法分辨哪句是它查过的、哪句是它编的，这比答不上来严重得多。现在加了两道闸：一是它不查就想作答时，顶回去让它先查；二是万一还是空手作答，凡是出现「没有」「还没有登记」「一个都没有」这类关于你数据的结论，直接拦下重答——**编的那一版你一个字都不会看到**
- 「我现在有什么渠道」「有什么渠道」「我有几个渠道」这类问法现在也会直接查库了。以前只认「渠道有哪些」这种把「渠道」放前面的说法，关键词在前的一律落给模型自己决定
- **「我有哪些待办」现在答得全了**。跟进计划和待办本来是两张表，它只看得见前一张；现在一起给，逾期的标出来
- **「团队里有哪些人」「我们有几个销售」**。以前够不着——按销售分组的统计里只有**出过业绩的**那几个人，刚入职的一个都不出现
- 「这周新增了哪些客户」「这个月预计能签哪几个」「超过 10 万的单子」「这个月要关的单子」也都能问了
- 让 Claude Code / Codex 连进来查的只读工具从十个变成十一个（多了团队名单）

## 0.37.2（2026-09-18）

这一版是把 AI「够不着的数据」接上——都是你问得出口、它却答不上来的问题。

- **「小红这个渠道里有谁」现在答得上来了**。以前只能告诉你有哪些渠道、各带来多少人，却列不出具体是谁。同一类问题一并接上：**「李四手上有哪些客户」**（以前只能问「我的」，问别人一律是空的）、**「大三的有哪些」**（年级不在搜索范围里）、**「还在犹豫的有谁」**（只能按跟进状态筛，决策状态没有入口）
- **「这个月签了哪几单、分别是谁」**。签约是这里最重要的一件事，以前却只有一个总额——问名单它只能回你「合计 ¥19800」。现在能按月份、按客户、按销售、按渠道列出每一笔：谁、多少钱、哪天签的
- **客户档案里带上联系人了**。家长、对接人的电话和微信本来就存在库里，AI 读档案时却看不到——问「张三家长的微信是多少」，它会如实告诉你没有，而那条数据一直都在
- **修：直接报姓名让它读档案，它说查不到**。「看看张三的情况」这种最自然的问法，从 0.37.0 起一直是坏的——它只认内部 id。现在认姓名了，重名会把候选列出来让你挑，不替你猜
- 让 Claude Code / Codex 连进来查的只读工具从九个变成十个（多了签约名单）

## 0.37.1（2026-09-18）

- **修：0.37.0 的桌面端装上打不开**（启动就弹 `Cannot find module './mcp-bridge'`）。新加的那个文件没写进打包清单，`app.asar` 里就没有它。0.37.0 的桌面包已从更新里撤回，请直接更到这一版；网页版和自部署不受影响
- 补了一条用例：壳里 `require` 的每个本地文件都必须在打包清单里，漏了就在 CI 红——本地跑 `npm run dev` 不走 asar，这类问题只会在装出来的应用上暴露

## 0.37.0（2026-09-18）

这一版是关于「问它一句话」这件事的：能翻回去看、答得对、答得快，以及——你可以不用我们的 AI。

- **首页的对话能翻回来了**。在这之前那串问答只活在内存里：刷新一下就没了，也开不出第二个——想把「这个月的回款」和「张三这条线怎么接」分开谈，只能两件事串在同一屏。现在左边多一列，问过的按今天 / 昨天 / 更早列着，点一条就把那次问答读回来，能重命名、能删。**只有自己看得见**：同一个工作区里别人（包括管理员）翻不到你的对话。提问时带的文件仍然只在内存里，不入库
- **答得对了**。AI 多了四类能查的东西：渠道、线索、商机、跨客户搜跟进记录。之前问「我目前的渠道有哪些」它其实够不着这份数据，只能拿「按渠道分组的客户数」硬凑——而没带来过客户的渠道根本不在那个结果里，答案必然是错的，它还要为此想很久
- **答得快了**。常见问题（「有哪些渠道」「这个月谁签得最多」「今天该跟谁」）现在**三到六秒**出结果，比之前快了一半：这类问题的答案是一条固定的查询，不必每次让模型重新想一遍该查什么
- **搜跟进记录会连同义词一起搜**。你问「谁提到过预算」，它会把「费用 / 学费 / 价格 / 报价」一起搜了——以前库里写的是「费用」，它只会回你「没找到，要不要换个词再搜一遍」
- **可以让 Claude Code / Codex 直接连进来查你的客户本**（MCP）。设置 → AI 接入 里生成一条命令，粘到终端执行一次，之后在那边问「张三最近跟进到哪了」，用的是**你自己**的订阅额度，数据一步不出这台机器。开出去的是九个**只读**查询，写不了任何东西
- 修：客户页「生成简报」按钮上看不见的字；问完之后首页中间空一大片

## 0.36.6（2026-09-18）

- **首页的对话能翻回来了**。在这之前那串问答只活在内存里：刷新一下就没了，也开不出第二个——想把「这个月的回款」和「张三这条线怎么接」分开谈，只能两件事串在同一屏。现在左边多一列，问过的按今天 / 昨天 / 更早列着，点一条就把那次问答读回来，能重命名、能删。**只有自己看得见**：同一个工作区里别人（包括管理员）翻不到你的对话。提问时带的文件仍然只在内存里，不入库
- **AI 能查的东西多了四类**：渠道、线索、商机、跨客户搜跟进记录。之前问「我目前的渠道有哪些」它其实够不着这份数据，只能拿「按渠道分组的客户数」硬凑——而没带来过客户的渠道根本不在那个结果里，答案必然是错的，它还要为此想很久
- **问完之后那一屏不再空一大片**：问答改成贴着输入框往上长

## 0.36.5（2026-09-18）

- **一个人用的时候，不再问「负责人」**。桌面端是一人公司或者一个销售自己记账，库里只有你一个人——客户、线索、渠道、商机四个表单里的负责人整项都不出现，保存时自动就是你。团队里有第二个人时照旧要选；编辑一条挂在别人名下的旧记录时也照旧问
- **完成的跟进计划和待办能看见了**。点完成之后它原来就从界面上彻底消失，「上周排的那次回访到底做没做」没有地方答得上来。跟进计划页顶上多了「待办 / 已完成」，已完成按完成时间倒序，每行写着原定什么时候、实际什么时候做完
- **修：客户页「生成简报」那颗按钮的字看不见**（灰字压在蓝底上）

## 0.36.4（2026-09-18）

- **一个人用的时候，负责人下拉不再是空的**。桌面端（以及刚注册的网页工作区）库里只有你一个人、而你是管理员，「管理员不做销售」那条规则把你自己排除在外了——于是**新建渠道整条路走不通**：渠道负责人是必填的，下拉却写着「暂无数据」。同一个根因还让三处失灵：跟进页按成员筛选选不出人；首页让 AI 记一笔并指派负责人时会说「没有叫 XX 的在职销售」；数据页的业绩排行榜在单人库里永远是空的，你自己的业绩从来没显示过
- 多人团队的行为没有任何变化：管理员照样不进负责人候选、也不上业绩排行榜。只有「整个工作区一个非管理员都没有」时这条规则才让路——那时那个人就是销售本人

## 0.36.3（2026-09-18）

- **多了一套「外贸出口」预设**。设置 → 业务配置 顶上点一下，整组措辞和选项一次填好：线索来源是阿里国际站、展会、独立站询盘、邮件开发、领英、小红书、老客户返单；行业是外贸 / 进出口、跨境电商、代理经销、工程承包这些；职位是采购负责人、项目经理、跟单。状态显示名也跟着改：「已试听」叫「已寄样」，「已加微信」叫「已建联」，「已决定报名」叫「已决定下单」——存储值一个没动，统计和盯盘照旧
- 预设仍然只是**填表的快捷方式**：点完看一眼再点保存，每一项照样能自己改；已有的客户和线索不受影响，变的只是新录入时能选到什么

## 0.36.2（2026-09-18）

- **桌面端不再有「灌一套演示数据」**。桌面端是你自己的库，装完就该录自己的第一位客户，一屏之内摆一颗「灌一套假数据」只会让人分不清哪些是真的。网页版（团队自己架的那套）保留——那边常常是一群人先看看它长什么样，再决定要不要用。之前在桌面端灌过的，「清除演示数据」照样在
- **操作日志补齐了**：商机（新建、改阶段、赢单丢单、删除）、跟进记录、待办、跟进计划、联系人、新建线索，现在都留痕。之前删掉一整条几万块的商机，日志里一个字都没有
- **第一次打开的首页**：上面那张「开始」卡和下面的输入框现在一样宽
- **设置 → 业务配置**：业务简介底下那段说明不再被右下角的字数压住（两行字叠在一起，看上去像乱码）
- **设置 → AI 接入**：第二项改叫「用你自己的 API Key」

## 0.36.1（2026-09-18）

- **「清除演示数据」不再找不到**。它原来只长在空状态里：灌完之后列表不空了，那张卡片就再也不出现，想清掉只能删库文件。现在「设置 → 业务配置」底下也有一个

## 0.36.0（2026-09-18）

默认不再是教培的说法了。

- **默认措辞改成通用销售**：客户 / 公司 / 职位 / 行业。之前默认是教培招生那套（学员 / 院校 / 年级 / 专业），非教培的团队打开要先去设置里改一遍，而多数人不知道那儿能改
- **教培招生变成一个预设**：设置 → 业务配置 顶上点一下「教培招生」，整组措辞一次填好，再点保存。做外贸、做 SaaS、做服务的同理，改完是你自己的词
- **跟进状态里几个教培味的说法换了**：已试听 → 已演示，已决定报名 → 已决定采购，与家人商议 → 内部讨论。这只是界面上的叫法，你自己改过的不受影响
- **AI 跟着一起换语境**了。之前即便你改了措辞，速记解析背后仍然在用「试听」「与家人商议」这套词理解你的对话
- **桌面端不再有「团队成员」那一栏**：桌面端是一个人用的，那一栏只会列出你自己，却摆着一个「新增成员」——在本机加出来的人没有云端账号，登不进任何地方。要多人一起用，用「连接服务器」
- 演示数据、官网和 README 上的十四张截图全部换成了新界面和通用数据

## 0.35.2（2026-09-18）

两个 bug。

- **改过的名字不会再被改回去**。在「个人资料」里改完名字、重新登录一次就变回原样——桌面端每次登录和每次启动都会把你的名字「对成」云端账号那个，而且是无条件覆盖。现在只有你没改过的时候它才跟着对；登录邮箱仍然一直对着账号，那是账号的身份
- **打开一位学员不再自动生成简报**。原来是进页面就先问一次 AI：随手点开三个人看看电话，三次额度就没了（免费额度一个月 30 次）。现在那儿是一颗「生成简报」按钮，旁边写明它会用掉 1 次；之前生成过的还是照样直接显示

## 0.35.1（2026-09-18）

上一版的动效太轻了，几乎感觉不到；顺手修了弹框的位置和设置浮层会蹿高。

- **按下去的反应加大了一档**。上一版一条导航项按下只缩 2.9px——量得出来，看不见；现在是 6.8px。按钮、图标键同理
- **卡片指上去会浮起来**（抬 3px + 一层浅阴影），不再只是换个底色
- **登录页的内容依次出现**，标志先落定；换页的过渡也比上一版明显
- **所有弹框都居中了**。之前它们钉在离屏幕顶端 100px 的地方，窗口越高越偏上，而你的鼠标多半在屏幕中间
- **弹框不再"砸"出来**：原来是从五分之一大小放大到满，现在只从 97% 长到 100%
- **设置浮层不再随内容蹿高**。之前切到「业务配置」「桌面端」这种长栏，整层会当场变大一截；现在框是固定的，内容在里面滚，各栏宽度也统一了
- 系统开了「减弱动态效果」的话，以上位移一律不发生

## 0.35.0（2026-09-18）

整套界面的手感重做了一遍：按下去有回应，换页有过渡，危险的那一下要专门做。

- **按下去的东西会动一下**。按钮、导航、标签、卡片——手指按下的瞬间缩一点点，松开弹回来。在这之前按下去界面是没有任何反应的
- **换页不再是硬切**：正文淡进来、抬一点点，左栏不动
- **登录、注册、找回密码**的内容按顺序出现，不再是整页一起砸下来
- **设置浮层是「长出来」的**，关掉回到原处
- **删一条跟进记录不再弹框**：那颗删除键自己变成「删除这条？ 删除 / 取消」。会连带删掉别的东西的那些操作（比如删学员、删签约）仍然弹框问清楚
- **清除演示数据改成滑动确认**：那一下会删掉全部业务数据，值得多做一个动作
- **桌面端下载更新时是一排刻度**，不再是一条线——走到哪儿一眼看得出
- **系统开了「减弱动态效果」的话，界面不再有任何位移**。之前只是动得快了，现在是真的不动

## 0.34.2（2026-09-18）

修好了「点设置没反应」，左栏能拖宽，多了一个发反馈的地方。

- **账号菜单里点「设置」，整行都能点**。上一版只有那两个字是可点的，点在图标上或右边空白上，菜单会关掉、什么也不发生
- **「个人资料」不在账号菜单里了**——它是设置里的第一栏。要改名字、改职位，打开设置，第一栏就是
- **⌘, 和菜单里点设置现在是同一个样子**：都是盖在当前页上的那一层，不再跳走整页
- **左下角多了一个反馈键**（手机版在顶栏）。用着别扭、哪儿出错了，写一句就能发过来。一起发的只有版本、系统和你当时在哪一页，不含任何学员、商机或跟进数据
- **左栏可以拖宽了**。抓右边那条缝拖，宽度这台机器记得住；双击回默认

## 0.34.1（2026-09-17）

设置改成盖在当前页上的一层，左栏不再摆它；顺手修了下载进度会涨过 100%。

- **点「设置」弹出一层浮层，不再跳走**。关掉就回到你刚才那一页，手里的事不被打断。地址还是老样子，能分享、能收藏，后退键就是关闭
- **左栏不再有「设置」**。那一列是你每天干活的地方——学员、商机、跟进；设置是偶尔去一趟的抽屉，摆在那儿等于每天提醒你它存在。它现在在左下角账号菜单里
- 账号菜单改成**点开**，不再是鼠标一路过就弹出来
- **下载进度不会再超过 100%**（有人看到过 126%）。算错的是分母：差量下载会把相邻的小文件并成一次请求，请求里那些文件之间的空隙也是真下了的字节，但没算进总数。顺带按钮上写的「差量更新 X MB」从此是真实下载量，不是偏小的估计

## 0.34.0（2026-09-17）

设置页重排了，你终于能自己改名字。

- **「个人资料」：改自己的名字和职位，不用再求管理员。** 在这之前，改名只存在于「团队成员 → 编辑成员」那个弹窗里，而它只有管理员打得开——一个销售想把名字从邮箱前缀改成中文名，得去找人。名字不是权限，是称呼：它出现在左下角、每条跟进的署名、业绩榜上。改完整站当场跟着变
- **左边的目录分成了「个人」和「工作区」两组**，上面多了个搜索框。项数多起来之后，最常见的一次来访是「我知道那个开关叫什么，但不知道它在哪一栏」
- **新增「快捷键」一栏**：⌘K 跳转、⌘N 新建、Enter 发送、Shift+Enter 换行、斜杠出命令、Esc 打断，桌面端再加 ⌘1–9 跳左栏和 ⌘, 开设置。这些键原来散在四处，不看源码没人知道它们存在
- **左下角那一行**：名字太长不再把整行吃掉，右边多了个小箭头——在那之前，它和一条静态的署名长得一模一样，看不出能点。点开的菜单也重做了：顶上写清现在是谁（名字、职位、登录名），下面只有个人资料、设置、退出登录
- 登录名仍然不能改：它同时是账号的标识，改一边不改另一边就登不进来

## 0.33.2（2026-09-17）

更新按钮不再自己占一行。

- 它现在待在侧栏最下面**账号那一行的右端**：平时一枚圆的下载键，鼠标放上去才变成「更新」两个字。没有新版时那一格根本不存在——不是藏起来，是没有
- 按钮上的字短了（更新 / 42% / 重启 / 重试），但鼠标停一下，版本号、要下多少、这一版改了什么都在

## 0.33.1（2026-09-17）

修一个打字打到一半就看不见的框，外加几处让人看得见"刚才发生了什么"的小动静。

- **首页输入框打了一大段字，现在整段都看得见**。原来不管打多少都只显示一行，前面的字被顶到框外——框本身没长高。长到大半屏就到顶，超出的部分在框里自己滚
- **更新按钮换了形状**：平时只是左下角一枚圆的下载键，鼠标放上去才摊开成「更新到 x」；点了之后一直摊着、底边走一条进度线；下完变成「重启以更新到 x」。有新版是邀请，不是通知，所以它平时不占地方（这一版装上之后才看得到新样子——你点着装它的那枚按钮还是旧的）
- **登录失败时，卡片会横着抖一下**。密码错了这件事，看见比读见快。错误提示也改成撑开的，下面的输入框跟着让位，位置不会跳一下
- **左栏那块蓝色不再是这边灭那边亮**，是同一块滑过去的——切页时眼睛跟着它走，不用重新找自己在哪一项上
- **新建完一条记录，它在列表里会亮两秒**。原来新建完只有一句「已创建」，人还得自己找刚填的东西落在哪儿。六张列表页都有
- 首页那三个数从 0 滚上来（每次打开只滚第一次）：它们是这一刻真查出来的，不是贴在那儿的图
- 等待时的灰色骨架屏晚 180ms 才出现——快的页面上你根本不会看见它。闪一下比多等一下更像出了问题
- 系统里开了「减弱动态效果」的话，上面这些一律不动

## 0.33.0（2026-09-17）

AI 那一栏只剩两个选择；首页那个框能带一个文件问了。

- **接 AI 不再是填六个字段，是做两个选择**：「用我们的」——走你登录的那个云端账号，界面上写着还剩几次，什么都不用填；「用我自己的 Key」——选一家（DeepSeek / 智谱 / Kimi / 通义 / OpenAI）、填一个 Key、挑个模型，地址和常用模型选完自动带出来，旁边有「去哪儿拿 Key」的直链。要填自己的接口地址就选「其它」
- **那句话现在写在界面上，它也确实是真的**：Key 加密存在这台机器上，请求由本机直接发给你选的那家，不经过我们
- 修了两处「把我们的当成你的」：「用自己的 Key」那一屏原来会显示一个尾号（其实是云端那把的），看着像你填过、留空一存就存进一个空 Key；模型也默认成了云端网关那个，拿它当 DeepSeek 的模型必然调不通
- **首页输入框照 Claude Code / Codex 的形状**：左下角回形针，右下角模型选择和发送键，都在框里，不再散在框外那行小字里
- **能带一个文件问了**。文件在浏览器里读成文字、跟着这一问发走——不上传、不落库、不写盘，这段对话本来就只活在内存里，刷新即清。所以「数据在你自己机器上」这句话带着文件问的时候仍然成立：它除了被发给你选的那个模型，不去任何地方。只收文本（txt / md / csv / tsv / json / log），单个 100 KB，一次最多 3 个；Excel 和 PDF 不收——解出来的未必是你以为的那份，不如自己另存成 csv，至少你知道发出去的是什么
- **设置页切页签不卡了**。原来换一个页签等于让整页重新向服务端要一遍：200 条操作日志、成员、AI 配置，桌面端还要去云端问一次剩余次数。现在切页签不回服务端，一页只问一次云端
- 渠道页不再闪骨架屏：原来渠道有几个就往数据库跑几趟，随后又几乎重复地全表查了一遍
- 新建线索时负责人不再显示成一串 id。那一页原来只列「非管理员」，而默认填的是「我」——一个人用的时候你就是管理员，于是它没得可显示

## 0.32.0（2026-09-17）

桌面端只剩一套身份：云端账号。壳只是壳。

- **登录页就是云端账号的门**：邮箱或手机号 + 密码，「注册新账号」开浏览器去网页办（网页和桌面端认的是同一个账号），「忘记密码」在应用内走完。原来壳里另画了一个登录窗、网页里又有一个要本机随机密码的框——两套身份、两把密码，点了左下角「退出登录」就被锁在自己机器外面。那个随机密码从此没有任何地方会显示，也没有任何地方要你输
- **左下角「退出登录」回来了**，退的是云端账号：吊销这台机器的令牌，回到登录页
- **网页上改了密码，桌面端跟着退出**：改密码 = 这个账号的每一台机器都退出。桌面端在启动时和每次切回前台时问一句，失效就说清原因回到登录页——不会出现「应用照常开着、只有 AI 莫名其妙不响应」。在网页「已登录的机器」里单独退掉这一台也一样
- **设置页多了一栏「桌面端」**：账号和 AI 剩余次数、修改密码、退出登录、备份数据库、打开数据文件夹、查看服务日志、检查更新、连接服务器、复制诊断信息。原来这些散在系统菜单里
- **菜单照 Claude 桌面端那套**：应用 / 文件 / 编辑 / 显示 / 前往 / 窗口 / 帮助，全是标准项，账号相关的一条都不在菜单里。「设置…」⌘, 直接开设置页；「前往」里 ⌘1–⌘9 跳左栏那几项
- 登录、退出立刻生效，不再重启本地服务
- 左栏和首页的比例照 Claude 桌面端改了：左栏更宽（220）、一格更高、字大一档；首页没问过时整列居中，一个大字、一个输入框、一排可点的

## 0.31.1（2026-09-17）

修一个能把你锁在自己机器外面的按钮。

- 桌面端用本机数据时，账号菜单里**不再有「退出登录」**。在那种模式下退出没有任何意义——数据就在你自己机器上，不给谁挡住什么——而退出之后要输的是建库时随机生成的本机密码，你从没见过它，那一页也没有「忘记密码」可点（本机的服务发不出信）。真正要退的是云端账号，那条一直在应用菜单里
- 万一还是落到了登录页（老版本升上来、或者会话过期），现在会自己进去，不再把你拦在门外。只有本机数据库真的出了问题时才会画出登录框，并告诉你本机密码在「应用菜单 → 本机账号密码…」里

## 0.31.0（2026-09-17）

这一版只做一件事：**机器丢了，你现在有退路了。**

- 改密码从今往后 = **所有地方都要重新登录**。网页端其他设备上的登录状态作废，桌面端每一台已登录的机器也一起退出。原来改密码只作废网页那一半，桌面端那几枚设备令牌不受影响——于是机器丢了的人只剩这一根杠杆，而这根杠杆对它不起作用
- 设置页「登录与密码」里多了一栏**「已登录的机器」**：哪几台机器用这个账号登录着、什么时候登的、最近什么时候用过 AI，每一台后面一个「退出」。只丢了其中一台就退那一台，另外几台照常用着，不用为此改密码
- 桌面端会自己发现自己被退了：启动时、以及每次切回应用时问一句。发现失效就说清是怎么回事，再回到登录界面——不会出现「应用照常开着，只有 AI 莫名其妙不响应」。**断网时当作还有效**，飞机上照常用本机的 CRM
- 本机数据始终不受影响。退出的是「用我们的云端账号调 AI」这件事，你的客户、跟进、合同都在你自己机器上

## 0.30.0（2026-09-17）

前端改版收尾：对着设计稿把每一页重新核对了一遍，顺手补上三处「看得到数、却落不了地」的地方。

- 左边那列导航不再叫「学员管理」「商机管理」，就叫「学员」「商机」。「管理」两个字每一项都有，等于每一项都没有
- **中栏只在学员记录页出现了。** 首页原来那列「今天」、商机和跟进各自那张两行的子页目录都撤了：一个模块两个视图，不值得为它常占一列。商机的「管道 / 列表」和跟进的「计划 / 记录」挪到了各自页头上的按钮
- 六张列表页的「新建 X」全部挪到页头右上角，六页同一个位置。原来它埋在筛选栏里，每换一页都要重新找一遍
- 筛选栏没有「搜索」按钮了：下拉改了就生效，关键词回车或点清空就生效。「重置」只在真筛了东西时才出现
- 列表里的时间改成能比较的说法：今天、昨天、3 天前、9 月 10 日。原来是「09-10 13:19」，两行放一起要在脑子里换算才知道谁更久
- 首页那行信号改成「这是什么数 / 数 / 去哪看」三段：逾期跟进 2 · 先处理。问候底下补了日期，输入框下面的问题保底四到六条，而且条条都是能点出答案的具体问题
- **「数据」页那四个数换了。** 原来是线索总数、活跃客户数、本月新增商机、预测销售额；现在是本月签约、新增学员、进行中商机、逾期跟进——前四个说的是「库里有多少」，后四个说的是「今天要关心什么」。而且每一张卡整块可点，落到一个能把这个数重新数一遍的页面
- **签约趋势图的柱子能点开了。** 点一根柱子（或它下面的日期），右边列出这一段的每一笔：谁、谁签的、哪天、多少钱，点名字进他的记录页
- 联系人页可以直接「添加联系人」，第一格先选他是谁的联系人；另外能按关系和负责人筛
- 学员记录页去掉了「← 返回列表」：左边就是名单，换一个人点一下就换。名字底下多了一行状态：跟进状态、决策状态、预计或已签约
- 空库首页那张「开始」卡重排了：三步横着走完，主按钮是「灌一套演示数据」，底下写明不会覆盖已有数据
- 用户协议和隐私政策换成和产品同一套排版，左边多了一列章节目录，跟着你读到哪儿走
- 打错的地址（比如 /students）以前会掉进一张英文的 404 页，现在是中文的，并告诉你回哪儿
- 登录框在托管版叫「邮箱」、自部署叫「用户名」——它本来就是两种账号
- ⌘N：在当前页新建。它点的就是页头右上角那个主按钮，所以六张列表页、管道、计划、记录页都有
- 左栏底下那行「⌘K 跳转 / 提问」的提示撤了，挪到首页输入框下面——用它的地方在那儿。原来那个「待办」铃铛也撤了，逾期改由首页第一个信号「逾期跟进 N · 先处理」承担：一个带数字和去处的信号比一个只有小红点的铃铛准

## 0.29.0（2026-09-17）

前端改版第五批：系统页。

- 出错、404 和空状态现在长一个样，说的也是同样三件事：发生了什么、为什么、接下来做什么，再给一个能点的地方。出错页除了「重试」还多了「重新加载」；404 会告诉你多半是记录被删了，并把你送回列表按名字搜
- 订阅页不再写价格、人数和权益。定价还没定，页面上先写「待定」——一旦写了数字就等于替产品定了价
- 桌面端那个登录窗：颜色、字号、圆角改成和应用里同一套；邮箱或手机号格式不对当场就说，不白跑一趟服务器
- 桌面端登录时连不上服务器，以前是按钮一直转、一个字的提示都没有（那条网络错误没人接）。现在会说清是超时还是连不上，以及去查什么
- 登录成功会说清这台机器记住了什么：存的是一枚设备令牌不是密码，在菜单里退出登录会把它吊销

## 0.28.0（2026-09-17）

前端改版第四批：看数的地方并成一处，设置换成左目录，加上 ⌘K。

- 看数不再有三个地方。首页的看板、数据看板、数据复盘合成一页「数据」，顶上切「现在 / 本月 / 本年」：现在是此刻的状态，本月按天铺开，本年按月铺开。原来的「数据复盘」地址还在，会跳到「本年」
- 每个数都写明是按什么口径、哪一段时间算的
- 「问数据」和首页那个输入框现在是同一个：边框、字号、Enter 发送、⌘K 回到框里都一样。原来一个是对话框、一个是搜索框，看着像两种能力
- 按销售 / 渠道负责人拆出来的那两张表，点名字能落到对应筛选的学员列表——「这 12 万是哪几位签的」有地方看了
- ⌘K：首页和数据页上是把光标放回输入框；别的页面弹一张单子，打字筛页面、回车跳过去。一个页面都没匹配上时，第一条变成「问一句」，回车带着这句话去首页问。左栏底下常驻一行提示，快捷键不该藏着
- 设置改成左目录五项（团队成员 / 登录与密码 / AI 接入 / 业务配置 / 操作日志），每项底下一句话说清管什么；原来那条中栏撤了——一页上摆两列目录，人得先弄清它们有什么区别
- AI 接入：没测过连接就点保存会先拦一下。整套 AI 都走这套配置，地址或 Key 不对会一起失灵，而且要等下一个人去用才发现

## 0.27.0（2026-09-17）

前端改版第三批：剩下的六页照着学员列表的做法重来一遍。它们现在用的是同一个表格实现，密度、空状态、分页、列设置、批量工具条全站一个样。

- 线索、渠道、联系人、商机、跟进记录：都只摆六列左右，其余收进「列」里；每页的空状态各写各的第一步，不再是一句「暂无数据」
- 联系人的电话和微信并成一列，「关系」写的是他和这位学员的关系
- 跟进记录那一列内容是最宽的一列，标题和内容压在一行——原来两行一格，一屏少看四五条
- 商机页顶上那三张统计卡换成一行三个数（总额 / 进行中 / 加权预测），一条商机都没有时整行不出现；赢单、丢单收进行尾的「更多」
- 跟进计划改成按时间分三组：逾期（红）、今天、本周。原来是左右两张卡分「待办」和「跟进计划」，可早上打开这一页想知道的是哪些拖了、今天要做哪些，不是哪些是任务哪些是计划
- 点完成之后那一行留在原地半秒再消失，下面的行不会突然跳上来顶替位置
- 自己名下没有、团队里却有一堆时会说一声，不再三组都写着「空的」
- 商机管道：列头写着这一阶段压着多少钱；卡片只留名称、金额、负责人；拖错了有「撤销」；不用鼠标也能换阶段（选中卡片按 Enter 出菜单）

## 0.26.0（2026-09-17）

前端改版第二批：三张母版。首页、学员列表、学员记录三页定下全站的做法，后面几页照着抄。

首页
- 空库时不再是一屏 0 和一个空输入框，换成一张「开始」卡：录一位 → 问一句 → 让它记一笔，三步走完就知道它替你做什么
- 有数据时顶上是一行三个信号——逾期跟进、高意向、本月签约。每个点得进去，鼠标停上去说清这个数怎么算的
- 能直接点的问题挪到输入框底下了（原来在半屏之外），↑↓ 选、回车问
- 每次回答底下跟着两三个能接着问的，都是从这次回答真提到的人长出来的
- ⌘K 把光标放回输入框

学员列表
- 不再横着拖两屏：默认只摆六列（学员、院校·专业、跟进状态、预计签约、负责人、最近跟进），其余的收进右上角「列」里，勾了就记住
- 整行可点，点进记录页；右边那个「详情」按钮撤了——一行里不摆两条通向同一个地方的路
- 勾了行才出现批量工具条，并且写着「已选 N 条」。原来那几个按钮一直摆着、灰着
- 常用的筛选摆三个（搜一句、跟进状态、销售负责人），其余进「更多筛选」，正筛着几个写在按钮上

学员记录
- 左边多了一条窄名单（最近跟进的 50 位）：从一位切到另一位不用退回列表。⌘K 直接搜
- 这条名单同时从列表页撤掉了——列表已经是全宽的一张表，旁边挂一条同样内容的名单是把同一件事画两遍
- 窗口窄到 1440 以下名单收成抽屉，1180 以下 AI 也收成抽屉，各有一个按钮开
- AI 建议改档案时变成逐项：每一项写清「现在是什么 → 改成什么」，各有一个勾，底下是「确认选中 N 项」，⌘↵ 等于点它。原来只能整张一起确认，只想要其中一项的人只好整张忽略

到处都能看见的
- 侧栏底部多了「AI 任务」：问一句要十几秒，切去别的页面也看得见它跑完没有，点一条回到原来那个位置；答完里面有建议卡等着确认的会标出来

## 0.25.0（2026-09-17）

前端改版第一批：底座。这一批不改任何页面的做法，只把「字多大、灰多深、线多细、一行多高」统一成一套，所以全站哪一页都会觉得变了一点。

- 字变大了一档，也不再有小到看不清的：正文和表格 14，说明 13，最小 12。以前满屏都是 11px、11.5px、10.5px
- 浅灰不再当正文用：跟进记录那一列内容原本是 #9ca3af，在 1440 的屏上基本读不了，现在统一到和别处一样的说明灰
- 表格一屏能多看七八行：行高固定 40、表头 36。以前一行 48–52，一屏只有十行出头
- 选中的行有左边一条蓝杠，不再只靠一层浅蓝底——扫过去看得见了
- 学员、线索、渠道、联系人、跟进这五张列表不再套一层白卡：表格本身就是内容，那层卡只是把底色挤成一圈边
- 同一个浅蓝以前有四个版本（选中态、AI 底、标签底各拍一个），现在是同一个

## 0.24.3（2026-09-17）

- 差量更新真的只下变了的那几 MB 了：0.24.2 那次因为 Electron 把 app.asar 当目录、比对中途断掉，多下了 58 MB

## 0.24.2（2026-09-17）

- 更新不再自动下载：查到新版先在侧栏出一个「更新到 x · 差量 2.3 MB / 整包 161 MB」的按钮，点了才开始下；下完再变成「重启以更新」
- 整包下载断了会接着下（Range 续传 + 自动重试），不再从零重来；「重试」按钮也是接着上次的进度
- 第一次真正走差量更新的版本：从 0.24.1 升上来只下变了的文件

## 0.24.1（2026-09-16）

- 切回应用时也会查一次更新，不用等 6 小时或去点菜单
- 打错的地址、已经删掉的页面恢复 404，不再显示一个空壳
- 为差量更新做准备：这一版起发布包带上文件清单。**下一版开始**更新只下载有变化的部分（几 MB，不再是 160 MB）；这一版本身还是整包

## 0.24.0（2026-09-16）

- 左边那条「当下」栏（学员那一列）从侧栏点进去时不出现、要刷新才有，修好了
- 每个空页面不再只写「暂无数据」，改成告诉你这是什么、第一步该做什么
- 数据看板在没有数据时不再画四个 0 和一条平线，换成一张「从哪开始」
- 本地使用时可以一键灌一套演示数据看看它长什么样，随时能清掉（仅管理员）
- 首页把几个能直接点的问题摆出来，用法说明压成一行
- 正文和说明文字各大一号，浅灰调深，1440 屏上更好读

## 0.23.3（2026-09-16）

- 修：应用内更新在第二次时报「ENOTEMPTY … app.asar」失败。上一次更新留下的旧包里有 `app.asar`，Electron 把它当成目录、删不掉，连带把这次更新也拦下了。现在删得掉；就算删不掉也先挪到一边、照常更新

## 0.23.2（2026-09-16）

- **更新改成按钮式**：应用后台自己查、自己下，下完侧栏底部出现「重启以更新到 x.y.z」，点一下就装。不再弹对话框
- 顶上整条都能按住拖窗口了——之前只有中栏那一块能拖
- 上次下好没装的更新包会直接复用，不重下

## 0.23.1（2026-09-16）

- 没有功能变化。这一版的用途是让装着 0.23.0 的人第一次按「立即更新」——验证应用内更新这条链在真机上走得通

## 0.23.0（2026-09-16）

- **应用内一键更新**：有新版时点「立即更新」，应用自己下载、校验、替换、重启。不用再去官网、不用拖文件，也不会再被 Gatekeeper 拦（那一关只在第一次从浏览器装时过一次）
- **备份数据库…** 菜单：选个位置，拷出一份验过的完整库。之前文档写「备份就是拷文件」——开着的库直接拷可能缺最近的写入
- **帮助** 菜单：使用文档、反馈问题（诊断信息已经填好）、复制诊断信息、打开日志文件夹
- 应用自己出错会弹框，能一键复制错误；错误也记在 `logs/app.log`
- 修：「跳过这个版本」以前不生效，每次启动照样提示

## 0.22.1（2026-09-16）

- 图标四个角深浅一致，程序坞小尺寸下不再发灰发白
- 发版流程加了一道闸：tag 和版本号对不上直接拒绝构建。这一版第一次打包时就撞上了这个坑，会让所有人陷进「装完还提示有新版」的循环，已撤回重发

## 0.22.0（2026-09-16）

- 新图标：深蓝框直接就是图标的边，外面不再套一层白
- 侧栏：图标和名字排在同一行，底下能看到自己是谁
- 程序坞里那个多出来的、没有图标的格子没有了
- 顶上空白的地方按住就能拖窗口
- 网页试用版改成一个共享工作区、一套固定账号密码；演示区整套下线

## 0.21.x（2026-09-16）

- 图标栏把应用名写出来；共享工作区能灌演示数据
- 修：镜像构建打不出来（`scripts/` 被 `.dockerignore` 排除）

## 0.20.x（2026-09-15）

- 三栏壳：图标栏 / 当下栏 / 正文。首页中栏是「今天要跟的人和到期待办」
- 桌面端去掉系统标题栏，红黄绿钮嵌进图标栏
- 修：一个页面两个 `<main>`（读屏认不出正文）；检查更新改成「官网和 GitHub 谁新听谁的」

## 0.19.x（2026-09-15）

- 桌面端本地模式要先登录云端账号（免费、邮箱注册）。数据仍然只在你机器上，账号只用来记 AI 次数
- 注册完自动回到应用

## 0.18.x（2026-09-15）

- 官网「预约演示」表单直发邮箱；`/api/health` 探活；安全响应头
- 容器日志封顶；每五分钟一次的线上探活

## 0.17.x（2026-09-15）

- 开号只剩「注册」一条路：邀请码、激活码、演示码整套下线
- 桌面端注册跳网页；找回密码在应用内

## 0.9 – 0.16（2026-09-14）

- 托管版底座：多租户、注册、试用期、运营台（这一套在 0.22 改成了共享工作区）
- 建议卡覆盖档案 / 商机 / 签约；对话带上下文
- 渠道负责人可单独订正；改渠道不再追溯改写已有学员
- 修：推理模型的两个坑；留痕日期差一天；演示数据里有同名学员

## 0.5 – 0.8（2026-09-10 – 09-13）

- 首页改成命令行式的 agent 对话面：排队、打断、折叠过程、客户卡片
- AI 写入提议：建议卡同时是表单，缺必填项时确认钮是灰的，人点确认才落库
- 记录页三栏

## 0.1 – 0.4（2026-09-05 – 09-09）

- 开源首版：线索 → 客户 → 跟进 → 商机 → 签约，推荐归属自动算
- 零配置 `docker compose up -d`
- 打 tag 自动出多架构镜像
- 首页提问框；速记原文留存；盯盘一键 AI 解读
