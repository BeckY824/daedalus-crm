# Windows 版本验证记录

验证日期：2026-09-26。基于上游 `a87d085` / 0.46.5，运行环境 Windows 10 x64、Node 22.18.0、Electron 38.8.6。

## 已通过

- Next.js 生产构建、Windows 原生 Prisma 引擎装配、NSIS x64 安装包生成。
- 完整 Vitest 回归：95 个文件通过、1 个跳过；1,226 项通过、12 项按平台条件跳过。
- TypeScript 类型检查通过；ESLint 无错误，17 条已有警告。
- 打包后的 `win-unpacked/Daedalus CRM.exe` 实际启动与登录。
- 通过界面创建客户，访问客户、渠道、联系人、商机、跟进、报表和设置。
- 中文及空格数据路径、SQLite 在线备份。
- 连接远端服务器、切回本机数据、重启恢复登录和数据库。
- 两个账号切换后的数据隔离，切回原账号时原数据保留。
- Windows 更新源的平台筛选、安装包哈希校验与安装器启动参数测试。

桌面冒烟测试使用本地云端接口替身，不注册真实账号、不发送业务数据、不调用收费模型。测试脚本：`desktop/scripts/smoke-windows.mjs`。

## 交付物

- `desktop/dist/Daedalus-CRM-0.46.5-x64-setup.exe`
- 大小：119,075,120 字节。
- SHA-256：`6858ad3ffddcb908cb975ad9f53124fcb737019ad603293b48f75e7b1047a7ef`

## 用户试用反馈

用户随后反馈：「测试了，基本上功能都是好的。」这条记录表示本地安装包的基本功能已获用户试用确认；尚未提供逐项结果，因此不据此认定真实 AI、卸载或覆盖升级已完成验收。

## 自动化验证边界与发布状态

- 未在本机实际执行 NSIS 安装、卸载或覆盖升级；已验证打包目录中的真实程序。
- 未使用真实云端账号联调，未调用真实 AI。
- 未运行完整 Playwright 网页 E2E 套件；已运行上述桌面业务冒烟流程。
- 未在 Mac 真机重新构建；Mac 更新解析和桌面逻辑的可跨平台回归测试已通过。
- 未配置 Windows 发布者签名，未上传 Release，未修改另一个仓库中的官网更新 feed。

构建和发布接入说明见 [Windows版.md](Windows版.md)。
