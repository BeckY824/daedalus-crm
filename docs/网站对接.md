# 网站要对接的东西

桌面端和应用里有几处写死的对外地址，官网改版时不能动它们，否则装出去的客户端会静默失灵。
这一篇就是那份清单，外加各部分代码在哪。

## 一、官网必须提供的两样

### 1. `https://ai-daedalus.com/download.html`

下载页。桌面端「检查更新」发现新版时打开的就是它（`desktop/updater.js` 里的 `下载页`），
`latest.json` 不带 `url` 时也回落到它。

页面上至少要有：

- Mac（Apple 芯片）的 dmg 下载链接
- **放行 Gatekeeper 的步骤**。第一次打开一定会被系统拦，说明不写清楚的话这批人就流失在这一步。
  文案直接抄 [桌面端安装.md](桌面端安装.md) 的「装」一节
- 一句「只有 Apple 芯片版」。Intel Mac 现在没有包

### 2. `https://ai-daedalus.com/desktop/latest.json`

版本信息。桌面端每天自动查一次，也可以手动查。长这样：

```json
{
  "version": "0.45.0",
  "url": "https://ai-daedalus.com/download.html",
  "dmg":      "https://ai-daedalus.com/dl/Daedalus.CRM-0.45.0-arm64.dmg",
  "zip":      "https://ai-daedalus.com/dl/Daedalus.CRM-0.45.0-arm64.app.zip",
  "manifest": "https://ai-daedalus.com/dl/Daedalus.CRM-0.45.0-arm64.manifest.json.gz",
  "sha256": "1c067ead…",
  "备用": {
    "dmg":      "https://github.com/BeckY824/daedalus-crm/releases/download/v0.45.0/…dmg",
    "zip":      "…app.zip",
    "manifest": "…manifest.json.gz"
  },
  "notes": "这一版改了什么，会原样显示在提示框里",
  "size": "131 MB"
}
```

| 字段 | 谁写的 | 干什么 |
|---|---|---|
| `version` / `notes` | **手写**在官网仓库的 `desktop/latest.json` 里 | 要发哪一版、那句人话 |
| `dmg` / `zip` / `manifest` / `sha256` / `size` | 打包时由 `deploy/latest-json.py` 从 GitHub 的资产现取 | 下哪个包、多大、校验什么 |
| `备用` | 同上，开了镜像才有 | 上面那三个地址下不动时换的原址 |

- `version` 只看前三段数字，`v` 前缀可有可无
- 这个文件**优先级高于 GitHub Release**。改它就能立刻让所有客户端看到新版，
  适合控制放量；不想管它就别放，桌面端会自动回落到 GitHub
- 兜底源：`https://api.github.com/repos/BeckY824/daedalus-crm/releases/latest`，
  用 `tag_name` 当版本号。打 tag 时 CI 会把 dmg 挂上去，所以通常自动就是对的
- `zip` + `manifest` 齐了才能走差量，缺一个就只能整包（`desktop/delta.js`）

**关于镜像（2026-09-20 起）**：GitHub 在国内下得慢（几百 KB/s），所以同一份包在官网
服务器（香港）上也放一份，`dmg`/`zip`/`manifest` 指过去，GitHub 原址挪进 `备用`。

- 切不切由**打包时的探测**说了算：`latest-json.py` HEAD 探那三个文件，
  **都在、长度都和 GitHub 上那份一致**才切；缺一个就整份维持 GitHub 原址。
  宁可慢，也不要一半地址指向不存在的文件——那是「更新失败」，比慢难受
- **不新增故障域**：feed 本来就是这台服务器发的。它挂了，feed 也取不到，
  桌面端那时会自己退回 GitHub 的 Release 接口
- 老客户端读的就是 `dmg` 字段，所以**不用等升级**也能享受镜像；
  `备用` 是 0.46.0 起才会用的（`desktop/install.js` 的 `下载文件({url, 备用})`）
- 包放在 `/var/www/dl`，**故意在 webroot 外面**：官网发布是 `rsync --delete`，
  放进 `/var/www/ai-daedalus` 下次发站就被删光了。nginx 那条 `location /dl/`
  在官网仓库的 `deploy/nginx.conf` 里

> 注意：官网的部署脚本是**白名单**的。新增这两个文件要同时加进
> 官网仓库 `deploy/build.sh` 的 `FILES` 数组，否则不会被发到线上。

## 二、官网不能删的一页

`https://ai-daedalus.com/demo.html`

这一页的表单从 0.18.0 起直接 `POST https://app.ai-daedalus.com/api/lead`（见 `docs/托管版.md`「官网预约演示表单」）。
官网只认 2xx；其余任何结果都落回页内的复制粘贴兜底面板。表单里有个 `website` 蜜罐字段，
改版时**别删、别显示出来**，服务端靠它识别脚本。

应用内有一处链到它：试用横条的按钮。
改版可以改内容，但**路径要留着**，或者同步改
`src/app/demo/DemoGate.tsx` 与 `src/components/DemoBar.tsx`。

## 三、官网该往哪里送人

| 按钮 | 送到哪 | 说明 |
|---|---|---|
| 下载桌面端 | `/download.html` | 装完用邮箱注册个免费账号就能用（2026-09-15 起必须登录） |
| 注册账号 | `https://app.ai-daedalus.com/signup` | 手机号或邮箱 + 密码，不要验证码。桌面端现在**不能注册**，只能登录，所以想用 AI 的人要先到这里注册 |
| ~~在线演示~~ | `https://app.ai-daedalus.com/demo` | **2026-09-16 整套删掉**：路由、演示数据集、每晚重置接口、访客计数全部移除。它是全站唯一一个不校验密码就签发会话的地方，而网页版改成「一个共享工作区 + 一套固定账号密码」之后它也没有存在的理由了。现在想看网页版：联系我们拿那套账号 |
| 用户协议 | `https://app.ai-daedalus.com/terms` | 页面在应用里，官网页脚直接链过去即可 |
| 隐私政策 | `https://app.ai-daedalus.com/privacy` | 同上 |
| 自部署 | `https://github.com/BeckY824/daedalus-crm` | 开源版，docker compose 一条命令 |

## 四、桌面端连云端的地址

桌面端只在两件事上连云端：登录账号、调模型。地址写死在 `desktop/cloud.js`：

```
https://app.ai-daedalus.com/api/account/token       POST 发令牌 / DELETE 吊销
https://app.ai-daedalus.com/api/gateway/v1/models   可用模型
https://app.ai-daedalus.com/api/gateway/v1/credits  还剩几次
https://app.ai-daedalus.com/api/gateway/v1/chat/completions
```

换域名的话这里和 `desktop/main.js` 的 `默认服务器` 要一起改。
联调时用环境变量覆盖：`CRM_CLOUD_URL`、`CRM_URL`、`CRM_UPDATE_URL`。

## 五、代码在哪

CRM 仓库（就是这个仓库），下面的路径都相对仓库根目录：

| 路径 | 是什么 |
|---|---|
| `desktop/main.js` | Electron 主进程：窗口、菜单、本地/服务器模式切换 |
| `desktop/local-server.js` | 在用户机器上拉起随包发布的服务 |
| `desktop/server-entry.js` | 打进安装包的服务入口：首次建库、补迁移 |
| `desktop/cloud.js` | 云端账号：登录、退出、查额度、把令牌交给本地服务 |
| `desktop/updater.js` | 检查更新（只查不装）。feed 里的「备用」地址也在这儿解析 |
| `desktop/install.js` | 下载整包、校验、换包。`下载文件({url, 备用})` 那个备用就是镜像下不动时换的原址 |
| `desktop/delta.js` | 差量更新：拿清单和已装的包比，对 zip 发 Range 只下变了的段 |
| `desktop/sign.js` | ad-hoc 签名。Apple 芯片上不签会报「已损坏」 |
| `desktop/scripts/build-server.mjs` | 把 CRM 装配成随包发布的服务 |
| `desktop/server-bundle/` | 装配产物，不进版本库 |
| `desktop/dist/` | 安装包，不进版本库 |
| `src/app/api/gateway/v1/` | 模型网关 |
| `src/app/api/account/token/` | 发/吊销设备令牌 |
| `src/app/api/desktop/session/` | 本地模式的自动登录 |
| `src/lib/tenant/credits.ts` | AI 免费次数账本（工作区与账号共用一套规则） |
| `src/lib/tenant/device-token.ts` | 设备令牌 |
| `src/lib/gateway.ts` | 网关配置与请求体清洗 |
| `docs/桌面端安装.md` | 面向使用者的安装说明 |
| `desktop/README.md` | 桌面端的实现与取舍 |

官网是另一个仓库（静态站）：

| 路径 | 是什么 |
|---|---|
| `*.html` | 页面源文件，`en/` 是英文站 |
| `deploy/build.sh` | 打包到 `dist/`。**白名单**，新页面要加进 `FILES` |
| `deploy/deploy.sh` | 发布脚本，rsync 到官网服务器 |
| `deploy/mirror.sh` | 把某一版的三个安装包同步到官网服务器的 `/var/www/dl`（服务器自己从 GitHub 拉） |
| `deploy/latest-json.py` | 生成 feed：从 Release 取资产，探到镜像就把地址换过去 |
| `deploy/nginx.conf` | 官网 nginx，要和服务器上那份逐字一致。`location /dl/` 在这里面 |

> 服务器地址、登录方式、部署目录都不写进这个仓库——它是公开的。那些在运维笔记里。

使用者机器上的数据：`~/Library/Application Support/DaedalusCRM/`
（`data/crm.db` 业务数据、`data/.cloud.json` 云端令牌、`logs/server.log` 服务日志）。

## 六、发一版桌面端的完整步骤

1. 在 Apple 芯片的 Mac 上：`cd desktop && npm install && npm run dist:mac`
   （首次要手动 `node node_modules/electron/install.js`，本机 npm 禁用了安装脚本）
2. 产物在 `desktop/dist/*.dmg`
3. 打 tag 推上去，CI 会自己打包并把 dmg 挂到同名 GitHub Release
4. **同步镜像**（官网仓库）：`./deploy/mirror.sh 0.46.0`
   —— 让官网那台服务器自己从 GitHub 拉三个文件（23–26 MB/s，几秒钟），核对 sha256，再验直链和 Range
5. 改官网 `desktop/latest.json` 里的 `version` 和 `notes`，跑 `deploy/deploy.sh`
   —— **顺序不能和第 4 步反**：打包时探不到镜像，这一版就不开镜像（不报错，只是地址维持 GitHub）
6. 装出来自己走一遍：清掉 `~/Library/Application Support/DaedalusCRM` 再打开，
   应该是建库 → 自动进主界面
7. 发完开一下 <https://ai-daedalus.com/desktop/latest.json>，确认 `version` 是新的、
   `dmg` 指向 `/dl/`（开了镜像的话），`备用` 里是 GitHub 原址
