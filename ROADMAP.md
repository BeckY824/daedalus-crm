# 路线图

按"有人真的需要了再做"的顺序排。想推动某一项，开 issue 说清你的场景。

## 进行中：v0.4 → v0.8 换壳

对标 Attio 的动线，不照抄它的对象模型。分三步，每步单独可发：

- **v0.4 记录页三栏**：左档案点一下就能改、中一条时间线 + 顶部速记框、右 AI 常驻——打开谁它已读完谁。没有页签。
- **v0.5 对话首页与视觉体系**（本版）：首页只剩一个对话面，指标图表搬到「数据看板」；AI 任务在页面间持久；整套视觉换成白底细线中性灰，去掉大色块与标语；顶栏取消，搜索与用户块进侧栏。
- **v0.6 对话面收口**（本版）：一轮 = 右侧问题气泡 → 过程折成一句（点开看每步和模型的打算）→ 流式回答 → 涉及的客户卡片 → 复制 / 重试 / 移除；发送 / 停止钮，Esc、Ctrl+C 打断留「已中断」，正在答时再问自动排队；搜索工具改按姓名 / 学校 / 专业 / 备注关键词并返回总数（「武汉大学的有几位、分别是谁」能答）；决策步关思维链，快 5~10 倍。
- **v0.5 工作流可见**：每一问都能看见它在干什么——识别问题 → 读取记录（几条跟进、几段原文）→ 生成，一步一行像命令行的思考轨迹，跑完折成一行摘要；答案里每条说法带引用圆标，悬停看那条记录、在记录页点一下滚过去；答完直接给动作（起草话术 / 邀请）。
- **v0.7 列表页**：行内编辑、可保存的视图、筛选 chip，新建从模态框改为右侧滑出面板。
- **v0.8 ⌘K 与全站动画**：命令面板承接"问一位 / 问一个数 / 跳转"，记录切换连续滚入、AI 结果流式出现、状态变更过渡。

## 已发布

- **v0.3**：首页提问框（点到客户名出简报、否则问数据，两个快捷键替人挑客户）；速记原文留存到 FollowUpSource，简报 / 唤醒话术 / 盯盘解读引用原话；盯盘清单一键 AI 解读。仍然全部只起草不落库。

- **v0.2.1**：零配置启动——不传任何变量也能 `docker run`，会话密钥自动生成存卷，初始密码首启随机生成打印在日志。
- **v0.2**：跟进状态 / 决策状态的显示名可改（存储值不变）；设置页显示本月各 AI 功能用量；打 tag 自动构建多架构镜像推到 GHCR。
- **v0.1**：开源首版。

## 之后

- 合同管理、产品管理、目标管理三个模块（原设计稿里有，v0.1 去掉了入口）
- PostgreSQL 支持。代码层面只需换 Prisma datasource，但迁移脚本、备份脚本、WAL 相关的运维文档都是按 SQLite 写的，没验证过不敢写"支持"
- 桌面客户端（Electron 壳，首次启动填服务器地址）与 Windows 打包
- 自定义字段、自定义状态流转、多套行业模板

## 明确不做

- AI 代替人发消息、自动改状态、自动执行 workflow——agent 的工具全部只读，写永远由人在界面上点；「AI 起草，人拍板」是立场不是短板

- 自定义对象 / 自定义字段 / 自定义状态流转——Attio 那套对象模型是另一个项目，我们只学它的动线
- 泛化成通用 CRM——那是另一个项目
- 英文界面 / i18n——目标用户不需要
- 多租户——一个实例一个团队，"独立部署、数据归自己"是卖点不是缺陷
