# 路线图

按「有人真的需要了再做」排。想推动某一项，开 issue 说清你的场景，或去 Discussions 聊。
版本细节见 [CHANGELOG.md](CHANGELOG.md)。

## 现在是什么（0.35.2，2026-09-18）

三种用法，同一份代码：

| | 数据在哪 | 谁用 |
|---|---|---|
| **桌面端（Mac，Apple 芯片）** | 你自己的机器，一个 SQLite 文件 | 一个人或一台机器 |
| **自部署**（`docker compose up -d`） | 你的服务器 | 一个团队 |
| **网页试用版** app.ai-daedalus.com | 我们的服务器，一个共享工作区 | 想先试试的人，账号密码找我们要 |

已经有的：agent 对话首页（问一位、问一个数，每步可见）；AI 写入提议（建议卡即表单，人点确认才落库）；
记录页三栏、列表、看板、数据看板；推荐归属固化；手机号查重、并发按字段合并、全部写操作留痕；
AI 接入只有两个选择（用我们的云端账号，或自己的 Key：DeepSeek / 智谱 / Kimi / 通义 / OpenAI，任意中转站走「其它」）；
首页能带一个文本文件问，文件不上传不落库；教培措辞可改成任何销售团队。

界面这一层（0.25–0.35）：左栏常驻可拖宽、设置是一层浮层、⌘K / ⌘, / ⌘1–9、
全站按下即有回应（开了系统「减弱动态效果」则一处位移都没有）、左下角一个反馈口子。
**AI 一次都不自动跑**：记录页的简报改成点按钮才生成，页面加载不调模型。
桌面端更新是差量的，改了什么下什么。

## 进行中

- **只修不加，攒着发。** patch 攒着，minor 每周一个稳定版
- 等真机回话：0.35.2 的四件（改名不再被覆盖、简报按钮、弹框居中、按下的手感）
- **反馈这条链还没在真机上走通过一次**：桌面端 → 云端 `/api/feedback` 的转发只有托管版那半有测试覆盖

## 之后（按可能的顺序）

- **Intel Mac 包**：CI 上换一台 x64 runner 就能出；等有两个以上的人要
- **Windows 包**：配置在，没在真机验过
- **文档站**：`docs/` 里那九篇挂到官网
- **合同 / 产品 / 目标**三个模块：原设计稿里有，0.1 去掉了入口
- **PostgreSQL**：代码层只需换 Prisma datasource，但迁移、备份、WAL 那些运维文档都是按 SQLite 写的，没验证过不写「支持」
- **列表页**：行内编辑（不进详情页就能改一格）、可保存的视图

## 明确不做

- **AI 代替人写入**：agent 的工具全部只读，写永远由人在界面上点。「AI 起草，人拍板」是立场不是短板
- **自定义对象 / 自定义字段 / 自定义状态流转**：Attio 那套对象模型是另一个项目，我们只学它的动线
- **泛化成通用 CRM**：措辞可换，模型不换
- **英文界面 / i18n**：目标用户不需要（README 有英文版，界面没有）
- **自部署版的多租户**：一个实例一个团队，「独立部署、数据归自己」是卖点。托管版的多租户是另一套开关，自部署时一行都不执行
- **Apple 开发者签名**：不买（99 美元/年，2026-09-17 再次确认）。桌面端第一次打开要在「系统设置 → 隐私与安全性 → 仍要打开」放行一次，之后应用内更新不再弹
- **换国内服务器 + ICP 备案**：不做（2026-09-17 拍板）。服务器在香港，大陆访问偏慢是已知取舍；footer 没有备案号也照旧。这条不再作为待办出现
- **收款**：现在不接。自部署免费，托管版是试用通道，AI 次数按账本

## 已经做完的大版本

- **0.33** AI 接入压成两个选择；首页输入框带文件提问；设置切页签、渠道页、负责人三处真机修复
- **0.30 – 0.32** 前端改版收尾；改密码 = 所有机器退出、已登录的机器可单独退；桌面端只剩一套身份（云端账号），设置页「桌面端」栏
- **0.24 – 0.29** 应用内更新（先问再下、差量、续传）；前端五批改版
- **0.23** 备份数据库、崩溃落日志、帮助菜单
- **0.22** 网页试用版改成共享工作区；新图标；侧栏
- **0.20** 三栏壳，桌面端去掉系统标题栏
- **0.17 – 0.19** 开号只剩注册；桌面端本地模式登录云端账号；官网表单、探活、安全头
- **0.9 – 0.16** 托管版底座（多租户、注册、试用、运营台）
- **0.5 – 0.8** agent 对话首页；AI 写入提议；记录页三栏
- **0.1 – 0.4** 开源首版；零配置启动；多架构镜像；提问框与原文留存
