# 安全

## 报告漏洞

请不要在公开 issue 里描述漏洞细节。用 GitHub 的私密漏洞报告（仓库 Security 页 → Report a vulnerability），
我们会在 7 天内回复。

## 部署时你该知道的

- **会话密钥 `AUTH_SECRET`** 不传时首次启动自动生成并保存在数据卷 `/data/.auth-secret`（0600）。它同时用于加密存储的 API Key，换掉或丢掉它会让已保存的 AI Key 失效（重填即可），搬机器要带上 `/data`。
- **没有默认密码**。首次启动不传 `INIT_PASSWORD` 会随机生成一个，只打印在那一次容器日志里；日志能看到的人就能登录，看完请改掉。
- **只经 HTTPS 访问时把 `COOKIE_SECURE` 设为 `true`**。保留 http://IP 入口时它必须是 `false`，这意味着 Cookie 可能在明文链路上传输——只在内网这样用。
- 登录有按 IP 与账号的限流；反代必须正确传递 `X-Forwarded-For`（附带的 Caddyfile 已处理）。
- 所有人可见可改全部数据，这是刻意的设计（小团队不需要复杂权限），代价由「操作日志只增不删」来平衡。
- AI 功能会把客户的姓名、跟进记录发送给你配置的模型接口。用第三方接口前请确认它的数据政策；本地 Ollama 是不出网的选项。
